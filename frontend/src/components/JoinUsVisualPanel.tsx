import { JOIN_US_VISUAL_PANEL_CLASS } from "../lib/joinUsPageLayout";

export default function JoinUsVisualPanel() {
  return <aside className={JOIN_US_VISUAL_PANEL_CLASS} data-testid="join-us-visual-panel" aria-hidden />;
}
