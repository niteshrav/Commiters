/** Site content width — uses more horizontal space than a narrow marketing column. */
export const SITE_CONTENT_MAX_WIDTH_PX = 1360;
export const SITE_CONTAINER_WIDTH_CSS = "min(var(--max-width), 96vw)";

export {
  BREAKPOINT_COMPACT_PX,
  BREAKPOINT_STACK_PX,
  BREAKPOINT_TABLET_PX,
} from "./responsiveLayout";
