# Copy to `.env.local` only when running backend + admin locally:
# VITE_API_BASE_URL=http://localhost:4000
# VITE_ADMIN_URL=http://localhost:5174
#
# Website-only (no API): do not create .env.local — forms use email fallback.

